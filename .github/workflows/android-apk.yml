name: Android APK

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - "android-app/**"
      - ".github/workflows/android-apk.yml"

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"
      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: "8.9"
      - name: Build APK
        working-directory: android-app
        run: gradle assembleDebug --no-daemon
      - name: Prepare APK
        run: cp android-app/app/build/outputs/apk/debug/app-debug.apk Mezhgorod-calculator.apk
      - uses: actions/upload-artifact@v4
        with:
          name: Mezhgorod-calculator-APK
          path: Mezhgorod-calculator.apk
      - uses: softprops/action-gh-release@v2
        with:
          tag_name: android-latest
          name: Межгород — Android APK
          body: Установочный APK калькулятора межгородних поездок.
          files: Mezhgorod-calculator.apk
          make_latest: true
